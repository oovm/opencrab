# 协议层设计

本文档详细说明 OpenCrab 系统的协议层，即 protocols/ 目录下的 Skynet 协议模块。协议层是整个系统的基础设施，提供标准化的通信协议、数据类型定义和交互接口。

## 协议层概述

协议层是 OpenCrab 三层架构中的最底层，为上层核心层（crab-* 核心模块）和表现层提供基础服务和标准化交互协议。

### 协议层在架构中的作用

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           表现层（应用端）                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │ oh-my-crab  │  │ oh-my-empire│  │ oh-my-waifu │  │ oh-my-planet│      │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘      │
│                                                                             │
│  ┌─────────────┐                                                           │
│  │ oh-my-home  │                                                           │
│  └─────────────┘                                                           │
└─────────────────────────────────────────────────────────────────────────────┘
                            ↓ 调用
┌─────────────────────────────────────────────────────────────────────────────┐
│                        核心层（crab-* 核心模块）                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │  crab-agent  │  │crab-orchestr │  │crab-organiz  │  │ crab-config  │   │
│  │              │  │    ator      │  │   ation      │  │              │   │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘   │
│                                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                      │
│  │  crab-types  │  │ crab-bridge  │  │crab-workflow │                      │
│  └──────────────┘  └──────────────┘  └──────────────┘                      │
└─────────────────────────────────────────────────────────────────────────────┘
                            ↓ 使用
┌─────────────────────────────────────────────────────────────────────────────┐
│                        ← 协议层（Skynet 协议）→                               │
├─────────────────────────────────────────────────────────────────────────────┤
│  基础类型  认证协议  聊天协议  网关协议  记忆协议  服务协议                    │
│  桥接协议  通知协议  持久化协议  存储协议                                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 核心职责

协议层的核心职责包括：

1. **提供标准化的数据类型定义**：统一各模块间的数据交换格式
2. **定义通信协议**：规范模块间、服务间的交互方式
3. **提供基础安全机制**：身份认证、加密传输、权限控制
4. **支持去中心化通信**：主网-子网两级结构，对等网络通信
5. **抽象底层差异**：为上层提供统一的接口，屏蔽底层实现细节

## 协议模块详解

### 1. skynet-types - 基础类型定义

**职责与功能**：
- 提供 Skynet 协议层的所有共享类型定义
- 定义核心数据结构、枚举和常量
- 提供身份标识生成和处理机制
- 定义错误类型和统一的错误处理规范
- 提供 JSON-RPC 和 WebSocket 消息类型

**主要子模块**：
- `agent` - 智能体相关类型
- `bridge` - 桥接相关类型
- `chat` - 聊天相关类型
- `error` - 错误相关类型
- `id` - 身份标识相关类型
- `jsonrpc` - JSON-RPC 相关类型
- `memory` - 记忆相关类型
- `org` - 组织相关类型
- `resource` - 资源相关类型
- `subnet` - 子网相关类型
- `user` - 用户相关类型
- `utils` - 工具函数
- `websocket` - WebSocket 相关类型

**在架构中的作用**：作为所有其他协议模块的基础，提供统一的类型系统，确保各模块间的数据兼容性。

---

### 2. skynet-auth - 认证协议

**职责与功能**：
- 提供用户身份认证接口
- 管理权限和访问控制
- 处理会话管理和令牌
- 支持多种认证方式

**核心功能**：
- 用户认证流程定义
- 权限管理和授权机制
- 会话令牌生成和验证
- Token 生命周期管理

**在架构中的作用**：为整个系统提供安全的身份认证和权限控制基础，确保只有授权的用户和服务才能访问系统资源。

---

### 3. skynet-chat - 聊天协议

**职责与功能**：
- 提供实时聊天和会话管理
- 定义消息类型和格式
- 支持聊天历史记录
- 提供实时通信接口

**核心功能**：
- 会话（频道）管理
- 多种消息类型定义（文本、文件、图片等）
- 实时消息推送
- 聊天历史查询和检索

**在架构中的作用**：为 OpenCrab 的协作功能提供基础通信支持，包括项目频道、团队沟通等。

---

### 4. skynet-gateway - 网关协议

**职责与功能**：
- 提供 API 网关接口
- 处理请求路由和转发
- 实现负载均衡
- 执行安全验证

**核心功能**：
- 请求路由和分发
- 服务发现和负载均衡
- 请求验证和安全过滤
- API 版本管理

**在架构中的作用**：作为系统的入口点，统一管理外部请求，提供安全、高效的 API 访问通道。

---

### 5. skynet-memory - 记忆协议

**职责与功能**：
- 提供记忆存储和检索接口
- 定义记忆数据类型
- 支持记忆搜索和查询
- 管理记忆标签和分类

**核心功能**：
- 记忆数据的存储和读取
- 记忆向量搜索
- 标签管理和分类
- 记忆关联和上下文

**在架构中的作用**：为智能体提供记忆系统支持，使智能体能够记住历史交互、学习经验并形成长期记忆。

---

### 6. skynet-service - 服务协议

**职责与功能**：
- 提供通用服务接口定义
- 支持服务发现机制
- 实现健康检查和监控
- 管理服务注册和注销

**核心功能**：
- 统一的服务接口规范
- 服务注册与发现
- 健康状态监控
- 服务生命周期管理

**在架构中的作用**：为系统中的各类服务提供统一的注册、发现和通信机制，支持微服务架构。

---

### 7. skynet-bridge - 桥接协议

**职责与功能**：
- 提供外部系统集成接口
- 处理消息转发和协议转换
- 支持多平台集成
- 管理跨系统通信

**核心功能**：
- 外部系统桥接
- 协议转换和适配
- 消息路由和转发
- 多平台连接器

**在架构中的作用**：使 OpenCrab 能够与外部系统（如第三方服务、遗留系统等）进行集成和通信。

---

### 8. skynet-notification - 通知协议

**职责与功能**：
- 提供消息通知和推送服务
- 定义通知类型和格式
- 支持多种推送渠道
- 管理通知历史记录

**核心功能**：
- 通知服务接口
- 多渠道推送（移动端、桌面端、邮件等）
- 通知类型定义
- 通知历史和状态管理

**在架构中的作用**：为系统提供实时通知能力，确保用户及时获取重要信息和更新。

---

### 9. skynet-persistence - 持久化协议

**职责与功能**：
- 提供数据仓库和持久化接口
- 定义实体类型和仓库模式
- 支持数据查询和事务管理
- 提供统一的数据访问层

**核心功能**：
- 仓库接口定义
- 实体类型规范
- 数据查询和过滤
- 事务管理和一致性保证

**在架构中的作用**：为系统提供统一的数据持久化抽象，屏蔽底层数据库差异，支持多种存储后端。

---

### 10. skynet-storage - 存储协议

**职责与功能**：
- 提供通用存储服务接口
- 支持多种存储类型（文件、Blob、对象等）
- 管理存储权限和访问控制
- 定义存储操作规范

**核心功能**：
- 通用存储服务接口
- 多类型存储支持
- 权限控制和访问管理
- 存储操作定义（上传、下载、删除等）

**在架构中的作用**：为系统提供统一的文件和对象存储抽象，支持各种存储需求。

## 协议层设计原则

### 1. 轻量化与灵活性

协议层只定义**最小必要**的接口和类型，保持轻量化和灵活性。具体实现细节由上层模块根据需求自主实现。

### 2. 标准化与一致性

所有协议模块遵循统一的设计规范和命名约定，确保各模块间的一致性和互操作性。

### 3. 可扩展性

协议设计预留扩展点，支持未来功能的添加和升级，而无需破坏现有接口。

### 4. 安全性优先

协议层内置基础安全机制，包括身份认证、加密传输、权限控制等，确保系统安全性。

### 5. 去中心化支持

协议层支持主网-子网两级结构，对等网络通信，为去中心化应用提供基础设施。

## 与上层的交互关系

### 核心层（crab-* 模块）

核心层的核心模块直接使用协议层提供的接口和类型：

- `crab-agent` 使用 `skynet-types` 和 `skynet-memory`
- `crab-organization` 使用 `skynet-types` 和 `skynet-persistence`
- `crab-orchestrator` 使用 `skynet-service` 和 `skynet-gateway`
- `crab-config` 使用 `skynet-types` 和 `skynet-persistence`
- `crab-bridge` 使用 `skynet-bridge` 和 `skynet-types`
- `crab-workflow` 使用 `skynet-service` 和 `skynet-types`
- `crab-types` 使用 `skynet-types` 作为类型基础

### 表现层（应用端）

表现层的五个应用通过核心层间接使用协议层，或在某些情况下直接使用协议层的基础类型和接口：

- `oh-my-crab` - OpenCrab 主应用，通过核心层模块访问协议层
- `oh-my-empire` - 组织管理应用，通过 crab-organization 访问协议层
- `oh-my-waifu` - AI 伴侣应用，通过 crab-agent 访问协议层
- `oh-my-planet` - 社区平台应用，通过核心层模块访问协议层
- `oh-my-home` - 个人中心应用，通过核心层模块访问协议层

## 总结

协议层是 OpenCrab 系统的基础设施，通过提供标准化的通信协议、数据类型定义和交互接口，为上层核心层和表现层提供坚实的基础。各协议模块各司其职，共同构成一个完整、灵活、安全的协议体系，支持系统的去中心化设计和企业级应用需求。

通过协议层的抽象，OpenCrab 实现了：
- 模块化设计，各部分可独立演进
- 标准化接口，便于集成和扩展
- 安全基础，保障系统安全性
- 去中心化支持，实现数据主权和用户控制
